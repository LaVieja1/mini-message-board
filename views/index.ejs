<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <a href="/new">Añadir un mensaje</a>
    <main>
      <ul>
        <% messages.forEach(message => { %>
          <li>
              <%= `${message.user}: ${message.text} (${message.added.toLocaleString()})` %>
          </li>
        <% }); %>
      </ul>
    </main>
  </body>
</html>
